# Database
DATABASE_URL=postgresql://user:password@localhost:5432/antipsych_tracker
DB_POOL_SIZE=5

# Encryption
FIELD_ENCRYPTION_KEY=
SECRET_KEY=
ALLOW_IDENTIFIERS=false

# Auth (Dev only - replace with OIDC in prod)
AUTH_MODE=dev_stub  # dev_stub or oidc
DEV_ADMIN_USERNAME=admin
DEV_ADMIN_PASSWORD=ChangeMe_123!
DEV_ADMIN_FORCE_CHANGE=true

OIDC_ISSUER_URL=https://login.microsoftonline.com/<tenant-id>/v2.0
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=

# Webhooks
WEBHOOK_SECRET=
WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS=600
IDEMPOTENCY_TTL_SECONDS=86400
REPLAY_TTL_SECONDS=600
RATE_LIMIT_MAX_PER_HOUR=100
RATE_LIMIT_BURST=20

# Notifications (v1: in-app only)
NOTIFICATIONS_ENABLED=false
EMAIL_ENABLED=false
EMAIL_SMTP_HOST=
EMAIL_FROM=
TEAMS_WEBHOOK_URL=

# Monitoring
TASK_WINDOW_DAYS=14
ESCALATION_THRESHOLD_DAYS=30
RETENTION_DAYS=90
SCHEDULING_HORIZON_YEARS=5

# Logging
LOG_LEVEL=INFO
LOG_JSON=true
AUDIT_EXPORT_PATH=

# Environment
ENVIRONMENT=dev  # dev, staging, prod
SYNTHETIC_DATA_MODE=true

# Redis
REDIS_URL=redis://localhost:6379/0
REDIS_REQUIRED=false
